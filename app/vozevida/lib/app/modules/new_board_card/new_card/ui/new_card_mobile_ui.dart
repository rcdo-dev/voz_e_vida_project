import 'package:flutter/material.dart';

class NewCardMobileUi extends StatelessWidget {
const NewCardMobileUi({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}