import 'package:flutter/material.dart';

class UserVoiceMenuMobileUi extends StatelessWidget {
const UserVoiceMenuMobileUi({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}